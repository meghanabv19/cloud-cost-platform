# secrets / local env
.env
.env.local
*.local

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
.user.yml

# data warehouse (hot store is rebuilt by the pipeline; never commit it)
data/*.duckdb
data/*.duckdb.wal
*.duckdb
*.duckdb.wal

# keep the frozen AWS CUR sample, ignore any other local parquet
/data/*.parquet

# misc
.DS_Store
