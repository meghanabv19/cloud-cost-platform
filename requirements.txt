# Dashboard runtime deps ONLY (this is what Streamlit Community Cloud installs
# from the repo root). The full pipeline deps live in requirements-pipeline.txt.
streamlit>=1.36
duckdb>=1.1
pandas>=2.1
pyarrow>=15
