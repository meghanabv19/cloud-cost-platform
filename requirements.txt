# Core (needed everywhere)
duckdb>=1.1
pandas>=2.1
pyarrow>=15
python-dotenv>=1.0
PyYAML>=6.0
requests>=2.32
tenacity>=8.2

# Cold archive (Cloudflare R2 via S3 API)
boto3>=1.34

# GCP (primary cost source)
google-cloud-bigquery>=3.20
google-cloud-billing>=1.13

# Transform (pin to stable 1.9 — avoids the dbt 2.0 "Fusion" pre-release)
dbt-core>=1.9,<1.10
dbt-duckdb>=1.9,<1.10

# Dashboard
streamlit>=1.36

# Tests
pytest>=8.0
